{
  "name": "webpack-backup-output-plugin",
  "version": "0.0.0",
  "description": "Webpack plugin to create backup of the output folder before files are emitted",
  "main": "index.es5.js",
  "jsnext:main": "index.js",
  "module": "index.js",
  "scripts": {
    "prepublish": "npm run transpile",
    "transpile": "babel index.js --out-file index.es5.js --no-babelrc --presets=es2015",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "webpack",
    "backup",
    "event",
    "emit",
    "output"
  ],
  "author": "Toke Voltelen",
  "license": "MIT",
  "dependencies": {
    "colors": "^1.1.2",
    "date-format-lite": "^0.9.2",
    "fs-extra": "^3.0.1"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-es2015": "^6.24.1"
  }
}
